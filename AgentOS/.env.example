# Copy this file to `AgentOS/.env` for local real-key testing.
# The current AgentOS e2e loader reads these OPENROUTE_* variables from `.env`
# and maps them into the runtime environment used by LiteLLM.

OPENROUTE_BASE_URL="https://openrouter.ai/api/v1"
OPENROUTE_API_KEY="your-openrouter-api-key"
